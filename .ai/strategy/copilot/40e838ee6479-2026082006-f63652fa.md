# Copilot strategy review

Status: `CHANGES_PROPOSED`

Static review of source commit 40e838ee6479f3eb0d844176a379ea85f9299200 found three bounded Strategy Lab improvements worth shadow-only follow-up: learned-pattern ranking currently overweights winners by averaging only positive proven trades, the Solana shadow lane still lacks current executable quote/liquidity evidence, and portfolio promotion logic still relies on a gross-loss profit-factor ratio even though fees and slippage are tracked separately. Fresh runtime evidence is not available, so this report does not claim profitability, canary readiness, or live readiness.

## IMPROVE — Learned Route Replication

A positive-only `avg_net_base` in `/home/runner/work/boot-trading-bot/boot-trading-bot/learnerbot/strategy.py` can over-rank route families whose occasional large losses erase many smaller wins. The Strategy Lab docs instead prioritise money-weighted net P&L, profit factor and largest loss. Shadow-only follow-up: recompute learned-pattern summaries from all proven net outcomes and compare out-of-sample post-cost results by chain.

## IMPROVE — Cross-chain Solana shadow executability evidence

`/home/runner/work/boot-trading-bot/boot-trading-bot/learnerbot/market_feature_adapter.py` currently leaves Solana gross edge, liquidity and sellability fail-closed at zero and tags the gap with `needs_quote_feature_adapter`. Because the lab is explicitly cross-chain, Solana needs its own bounded quote/liquidity/sellability adapter before shadow evidence is comparable with EVM.

## IMPROVE — Strategy Lab portfolio evaluator

`/home/runner/work/boot-trading-bot/boot-trading-bot/learnerbot/strategy_lab.py` already tracks fees, slippage and net profit, but its `profit_factor` remains a gross-profit/gross-loss ratio. Shadow-only follow-up: compare the current evaluator with a cost-adjusted variant and keep the stricter version only if later out-of-sample post-cost net P&L and loss control improve.

## Rejected ideas

- Do not claim canary/live readiness from this review alone; fresh runtime evidence is explicitly stale and shadow scorecards are not realised P&L.
- Do not optimize by leaning harder on single-wallet copying; the repository repeatedly prefers multi-wallet, pattern-based and market-native hypotheses.

## Evidence gaps

- Fresh runtime evidence within 2 hours is false.
- No local sanitized evidence bundle was provided in the workspace to replay realised participation or realised post-cost P&L.
- Static source review cannot prove any strategy remains positive out of sample across separate Solana and EVM regimes.
