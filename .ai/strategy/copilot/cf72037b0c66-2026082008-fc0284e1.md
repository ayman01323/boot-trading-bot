# Copilot strategy review — cf72037b0c66-2026082008-fc0284e1

- Status: `CHANGES_PROPOSED`
- Source commit: `cf72037b0c66d0f39ac92ee8c67699a7b98b9723`
- Evidence SHA-256: `fc0284e19819f952d521477192d232b84615314f0638091cb150d1af824ff891`
- Fresh runtime evidence within 2h: `false`

Architecture review is complete. The Strategy Lab already keeps research report-only, shadow-only and net-P&L-aware, but two bounded improvements are warranted before stronger cross-chain conclusions: finish a current-executable Solana quote/liquidity/sellability adapter for the shared shadow lane, and tighten lifecycle promotion scoring so downside gating reflects costs instead of gross-only profit factor. Because fresh runtime evidence was not available within 2h, this review makes no profitability, canary or live-readiness claims.

## Proposal 1 — IMPROVE: Cross-chain Strategy Lab feature coverage

- Evidence:
  - `learnerbot/market_feature_adapter.py` fail-closes Solana `gross_edge_bps`, `liquidity_score` and `sellability_score` to zero and marks `needs_quote_feature_adapter=True`.
  - `learnerbot/shadow_strategy_executor.py` reports `solana_current_quote_edge_adapter` and related evidence gaps while keeping quote/simulation output research-only.
  - `learnerbot/cross_chain_strategy_signals.py` runs all strategy families through one shared `MarketFeatures` schema, so missing Solana current-edge inputs block cross-chain comparability.
- Why:
  - EVM opportunities can already carry current executable edge, while the Solana lane still carries historical leader context without equivalent quote/liquidity/sellability evidence.
- Shadow test:
  - Over at least 3 hourly windows and 20+ Solana shadow observations per targeted family, compare pre/post adapter evidence gaps, eligible-opportunity counts and later realised-outcome alignment. Reject the change if the new adapter cannot keep quotes bounded/fresh or does not improve cost-adjusted attribution.

## Proposal 2 — IMPROVE: Strategy Lab lifecycle scoring

- Evidence:
  - `docs/STRATEGY_LAB.md` defines the objective as money-weighted net profitability after recorded costs.
  - `learnerbot/strategy_lab.py` records `fees`, `slippage_cost` and `net_profit`, but `_pf()` still uses `gross_profit / gross_loss`, and promotion requires `net > 0` plus that gross-only profit factor.
  - `tests/test_strategy_lab.py` proves net-negative strategies are replaced even with more wins, but does not yet prove that thin-margin strategies must clear a cost-adjusted downside gate before promotion.
- Why:
  - Gross-only profit factor can flatter strategies whose recurring execution costs consume most of the durable edge.
- Shadow test:
  - Replay historical `strategy_lab_windows` with a cost-adjusted profit-factor or downside-aware threshold and keep the change only if false-positive promotion candidates fall without materially increasing missed positive-net candidates over at least 3 windows and 8 trades per strategy.

## Rejected ideas

- Promote any strategy from quote-simulation scorecards or stale runtime evidence alone.
- Treat a single profitable leader wallet as the main strategy upgrade path.

## Evidence gaps

- Fresh runtime evidence within 2h is false, so this review cannot support profitability, canary or live-readiness claims.
- The exact hashed `.strategy_cycle/evidence.json` payload is not present in the workspace, so this is an architecture-only review of the named source commit plus issue metadata.
- Solana shadow inputs still lack current executable quote/liquidity/sellability evidence in the shared market-feature adapter.
