name: SiRisky One USD Safety Audit Google Only

on:
  workflow_dispatch:
  push:
    branches: [ops/google-master-menu-cutover-20260827]
    paths:
      - '.github/workflows/deploy-cold-zone-five-leaders-now-20260828.yml'

permissions:
  contents: read

jobs:
  audit:
    runs-on: [self-hosted, linux, x64, boot-google]
    timeout-minutes: 10
    steps:
      - name: Audit Jupiter builder and token cleanup tooling on botgoogle
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(hostname)" = botgoogle
          test "$(id -un)" = ayman01323
          tmp="$(mktemp)"
          trap 'rm -f "$tmp"' EXIT
          curl -fsSL --retry 3 --connect-timeout 10 \
            https://raw.githubusercontent.com/ayman01323/boot-trading-bot/d2afa8599dce319d27406ad7732e60d36afe19cf/scripts/sirisky_google_one_usd_safety_audit.sh \
            -o "$tmp"
          grep -q 'SIRISKY ONE USD SAFETY AUDIT V2' "$tmp"
          bash "$tmp"
