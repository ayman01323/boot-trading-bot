name: Finalize SiRisky Telegram Reporting Google Only

on:
  workflow_dispatch:
  push:
    branches: [ops/google-master-menu-cutover-20260827]
    paths:
      - '.github/workflows/deploy-cold-zone-five-leaders-now-20260828.yml'

permissions:
  contents: read

jobs:
  deploy:
    runs-on: [self-hosted, linux, x64, boot-google]
    timeout-minutes: 10
    steps:
      - name: Finalize BUY SELL and NewPoll45 Telegram reporting on botgoogle
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(hostname)" = botgoogle
          test "$(id -un)" = ayman01323
          tmp="$(mktemp)"
          trap 'rm -f "$tmp"' EXIT
          curl -fsSL --retry 3 --connect-timeout 10 \
            https://raw.githubusercontent.com/ayman01323/boot-trading-bot/721fc8c7b09f6e0a60054940bcd449755c70b439/scripts/sirisky_google_telegram_finalize.sh \
            -o "$tmp"
          grep -q 'FINALIZE SIRISKY TELEGRAM POSITION REPORTER' "$tmp"
          bash "$tmp"
