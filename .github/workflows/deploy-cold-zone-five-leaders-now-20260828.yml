name: SiRisky One USD Safety Audit Google Only

on:
  workflow_dispatch:
  push:
    branches: [ops/google-master-menu-cutover-20260827]
    paths:
      - '.github/workflows/deploy-cold-zone-five-leaders-now-20260828.yml'

permissions:
  contents: read

jobs:
  audit:
    runs-on: [self-hosted, linux, x64, boot-google]
    timeout-minutes: 10
    steps:
      - name: Audit fee construction and recoverable token rent on botgoogle
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(hostname)" = botgoogle
          test "$(id -un)" = ayman01323
          tmp="$(mktemp)"
          trap 'rm -f "$tmp"' EXIT
          curl -fsSL --retry 3 --connect-timeout 10 \
            https://raw.githubusercontent.com/ayman01323/boot-trading-bot/8388026ed82027ab8e3c577cc0f86f00b2c8d332/scripts/sirisky_google_one_usd_safety_audit.sh \
            -o "$tmp"
          grep -q 'SIRISKY ONE USD SAFETY AUDIT' "$tmp"
          bash "$tmp"
