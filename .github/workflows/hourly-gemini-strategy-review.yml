name: Hourly Gemini Strategy Review (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This standalone hourly Gemini workflow is intentionally disabled."
          echo "Gemini now runs inside Hourly Three-Agent Strategy Cycle so GPT, Gemini and Copilot review the same source/evidence."
