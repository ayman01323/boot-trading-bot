name: Polygon Readiness Check

on:
  pull_request:

permissions:
  contents: read

jobs:
  readiness:
    runs-on: [self-hosted, linux, x64, boot-vps]
    timeout-minutes: 5
    steps:
      - name: Current service status
        run: sudo /usr/local/sbin/status-boot-trading-bot
