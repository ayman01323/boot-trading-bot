name: Profit Guards Deploy Verify

on:
  pull_request:

permissions:
  contents: read

jobs:
  deploy-and-status:
    runs-on: [self-hosted, linux, x64, boot-vps]
    timeout-minutes: 20
    steps:
      - name: Deploy exact tested main commit
        run: sudo /usr/local/sbin/deploy-boot-trading-bot "7fd611ca677357d0c18c5fcba9a0b6ff186fa6ec"
      - name: Server status
        run: sudo /usr/local/sbin/status-boot-trading-bot
