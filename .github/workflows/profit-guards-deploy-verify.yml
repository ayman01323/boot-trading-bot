name: Profit Guards Deploy Verify

on:
  pull_request:

permissions:
  contents: read

jobs:
  deploy-and-status:
    runs-on: [self-hosted, linux, x64, boot-vps]
    timeout-minutes: 20
    steps:
      - name: Deploy exact tested main commit
        run: sudo /usr/local/sbin/deploy-boot-trading-bot "ad700bbf1ad7b1f566949b9793f98d4715d0320e"
      - name: Server status
        run: sudo /usr/local/sbin/status-boot-trading-bot
