name: Temporary SiRisky Restore 1240 Shadow 15pct 0090 20260828

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/temp-sirisky-restore-1240-shadow-15pct-0090-20260828.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  restore:
    runs-on: [self-hosted, linux, x64, boot-google]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Apply safe SiRisky shadow profile
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(hostname)" = botgoogle
          sudo -n bash scripts/restore_sirisky_1240_shadow_15pct_0090.sh
