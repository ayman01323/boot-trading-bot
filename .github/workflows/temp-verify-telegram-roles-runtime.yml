name: TEMP Verify Telegram Roles Runtime

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  verify:
    runs-on: [self-hosted, linux, x64, boot-vps]
    timeout-minutes: 5
    steps:
      - name: Read-only BOOT status
        run: sudo /usr/local/sbin/status-boot-trading-bot
