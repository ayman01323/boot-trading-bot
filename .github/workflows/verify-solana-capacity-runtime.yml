name: Verify Solana Capacity Runtime

on:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: [self-hosted, linux, x64, boot-vps]
    timeout-minutes: 5
    steps:
      - name: Current production status
        run: sudo /usr/local/sbin/status-boot-trading-bot
