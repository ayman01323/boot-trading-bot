BOOT v2.3 FULL POWER

LIVE AUTO now supports:
- V2 same-router triangular cycles on enabled V2 auto_execute venues.
- PancakeSwap V3 same-router exactInput triangular cycles on BSC, Base, Ethereum and Arbitrum.
- Every live route is re-quoted for the user's amount, gas/profit checked, then exact eth_call preflighted immediately before signing.
- Gas bid multiplier remains configurable and included in profitability simulation.

FAST PATH:
- Hot loop reads persisted V2/V3 pool registries and does no factory crawling.
- Chains are quoted in parallel.
- V3 seed-pool discovery runs in a separate background thread.

CROSS-DEX:
- Cross-DEX V2 candidates are detected but deliberately written enabled=false / SHADOW_ATOMIC_EXECUTOR_REQUIRED.
- The package includes contracts/AtomicV2ArbExecutor.sol as deployment/audit source only.
- The installer does NOT deploy a contract and does NOT enable sequential two-transaction cross-DEX execution.

POLYGON:
- QuickSwap V2 stays live.
- QuickSwap V3 Algebra addresses are recorded scan-only because Algebra's ABI/routing model is not silently treated as Pancake/Uniswap V3.
