# Allow the Google self-hosted runner user to invoke only the fixed-purpose
# root backup helper. The helper accepts no arguments and contains fixed paths.
ayman01323 ALL=(root) NOPASSWD: /usr/local/sbin/google-root-backup
