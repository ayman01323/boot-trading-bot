# Self-hosted CI smoke validation

Temporary branch-only probe used to verify that BOOT Python Tests allocate and execute on the restricted `boot-vps` self-hosted runner after the hosted-runner fallback change.
